<!-- Display the title if it exists, otherwise redirect to home -->
<% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<!-- Display messages if they exist -->
<%- message %>